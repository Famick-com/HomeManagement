<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               xmlns:popups="clr-namespace:Famick.HomeManagement.Mobile.Popups"
               xmlns:models="clr-namespace:Famick.HomeManagement.Mobile.Models"
               x:Class="Famick.HomeManagement.Mobile.Popups.ShareContactPopup"
               x:TypeArguments="models:ShareContactResult"
               Padding="10">

    <Border Padding="24" StrokeShape="RoundRectangle 16" Stroke="Transparent" StrokeThickness="0"
            BackgroundColor="{AppThemeBinding Light=White, Dark=#2A2A2A}" WidthRequest="320">
        <VerticalStackLayout Spacing="16">
            <Label Text="Share Contact" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" />

            <ActivityIndicator x:Name="LoadingIndicator" IsRunning="True" IsVisible="True"
                               HorizontalOptions="Center"
                               Color="{AppThemeBinding Light=#1976D2, Dark=#90CAF9}" />

            <CollectionView x:Name="UsersCollection"
                            HeightRequest="200"
                            SelectionMode="Single"
                            SelectionChanged="OnUserSelected"
                            IsVisible="False">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,2" Padding="12,8" StrokeShape="RoundRectangle 8"
                                Stroke="Transparent"
                                BackgroundColor="{AppThemeBinding Light=#FAFAFA, Dark=#1E1E1E}">
                            <Label Text="{Binding DisplayName}" FontSize="15" />
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label x:Name="SelectedUserLabel" FontSize="14" FontAttributes="Bold"
                   TextColor="#1976D2" IsVisible="False" />

            <Grid ColumnDefinitions="*,Auto">
                <Label Text="Can Edit" VerticalOptions="Center" />
                <Switch x:Name="CanEditSwitch" Grid.Column="1" />
            </Grid>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button Grid.Column="0" Text="Cancel" Clicked="OnCancelClicked"
                        BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                        TextColor="{AppThemeBinding Light=Black, Dark=White}"
                        CornerRadius="20" HeightRequest="44" />
                <Button Grid.Column="1" Text="Share" Clicked="OnShareClicked"
                        BackgroundColor="{AppThemeBinding Light=#1976D2, Dark=#1565C0}"
                        TextColor="White" CornerRadius="20" HeightRequest="44" />
            </Grid>
        </VerticalStackLayout>
    </Border>
</toolkit:Popup>
