<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Famick.HomeManagement.Mobile.Pages.AisleOrderPage"
             Title="Edit Aisle Order">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Done"
                     Clicked="OnDoneClicked" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- Header Info -->
        <Border Grid.Row="0"
                Padding="15,10"
                StrokeShape="RoundRectangle 0"
                Stroke="Transparent"
                StrokeThickness="0"
                BackgroundColor="{AppThemeBinding Light=#F2F2F7, Dark=#1C1C1E}">
            <Label Text="Hold and drag to reorder aisles."
                   FontSize="13"
                   TextColor="{AppThemeBinding Light=#8E8E93, Dark=#8E8E93}" />
        </Border>

        <!-- Loading Indicator -->
        <ActivityIndicator x:Name="LoadingIndicator"
                           Grid.Row="1"
                           IsRunning="False"
                           IsVisible="False"
                           VerticalOptions="Center"
                           HorizontalOptions="Center" />

        <!-- No Aisles Message -->
        <VerticalStackLayout x:Name="NoAislesView"
                             Grid.Row="1"
                             VerticalOptions="Center"
                             HorizontalOptions="Center"
                             Padding="40"
                             IsVisible="False">
            <Label Text="No Aisles Found"
                   FontSize="20"
                   FontAttributes="Bold"
                   HorizontalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light=Black, Dark=White}" />
            <Label Text="Aisle information is added as products are added to your shopping lists."
                   FontSize="15"
                   HorizontalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light=#8E8E93, Dark=#8E8E93}"
                   Margin="0,10,0,0" />
        </VerticalStackLayout>

        <!-- Reorderable Aisle List -->
        <CollectionView x:Name="AisleList"
                        Grid.Row="1"
                        SelectionMode="None"
                        CanReorderItems="True"
                        ReorderCompleted="OnReorderCompleted"
                        BackgroundColor="{AppThemeBinding Light=#F2F2F7, Dark=#000000}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0" HeightRequest="44"
                          BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Aisle Name -->
                        <Label Grid.Column="0"
                               Text="{Binding DisplayName}"
                               FontSize="17"
                               VerticalOptions="Center"
                               Padding="16,0,0,0"
                               TextColor="{AppThemeBinding Light=Black, Dark=White}" />

                        <!-- Reorder Handle (three lines) - iOS standard -->
                        <Label Grid.Column="1"
                               Text="≡"
                               FontSize="22"
                               TextColor="{AppThemeBinding Light=#C7C7CC, Dark=#48484A}"
                               VerticalOptions="Center"
                               Padding="0,0,16,0" />

                        <!-- Bottom separator line -->
                        <BoxView Grid.ColumnSpan="2"
                                 HeightRequest="0.5"
                                 VerticalOptions="End"
                                 HorizontalOptions="Fill"
                                 Margin="16,0,0,0"
                                 Color="{AppThemeBinding Light=#C6C6C8, Dark=#38383A}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Bottom Action Button -->
        <Grid Grid.Row="2" Padding="16"
              BackgroundColor="{AppThemeBinding Light=#F2F2F7, Dark=#000000}">
            <Button x:Name="ResetButton"
                    Text="Reset to Default Order"
                    Clicked="OnResetClicked"
                    BackgroundColor="Transparent"
                    TextColor="{AppThemeBinding Light=#007AFF, Dark=#0A84FF}"
                    FontSize="17"
                    BorderWidth="0" />
        </Grid>
    </Grid>
</ContentPage>
